{
  "name": "backend",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:migrate": "npx sequelize-cli db:migrate",
    "db:seed:all": "npx sequelize-cli db:seed:all",
    "dev": "PORT=3000 nodemon",
    "start:app": "node .",
    "start": "npm run db:migrate && npm run db:seed:all && npm run start:app"
  },
  "keywords": [
    "backend"
  ],
  "author": "Tarsis de Lima (https://github.com/tmvdl)",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.7",
    "sequelize-cli": "^6.2.0"
  },
  "dependencies": {
    "parseurl": "^1.3.3",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.5.0"
  }
}
