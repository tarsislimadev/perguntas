version: '3'

services:
  database:
    build: ./database
    ports:
      - "5432:5432"
    networks:
      - perguntas
  backend:
    build: ./backend
    depends_on:
      - database
    ports:
      - "8080:80"
    networks:
      - perguntas

networks: 
  perguntas:
